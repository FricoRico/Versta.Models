# Shared Ruff configuration for all Versta.Models modules.
# Formatting matches the previously used Black settings.

line-length = 88
target-version = "py312"

# Tooling/agent workspace files, not project code.
extend-exclude = [".agents", ".swarm"]

[lint]
select = ["E4", "E7", "E9", "F"]
